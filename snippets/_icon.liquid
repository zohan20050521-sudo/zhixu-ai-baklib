{%- comment -%}
  Lucide icon placeholder (replaced by application.js createIcons).
  @param name {String} kebab-case icon name (required)
  @param class {String} optional CSS classes
{%- endcomment -%}
<i data-lucide="{{ name }}"{% if class != blank %} class="{{ class }}"{% endif %} aria-hidden="true"></i>
